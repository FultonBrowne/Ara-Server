version: "3.0"
services:
        web:
                image: "fulton/ara-server"
                ports:
                        - 80:80
        auth:
                image: ""
                ports:
                        - 8080:8080
                enviroment:
                        - KEYCLOAK_USER=araadmin
                        - KEYCLOAK_PASSWORD=lol
                        

